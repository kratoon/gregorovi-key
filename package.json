{
  "scripts": {
    "build": "mkdir -p build/meta ; rm build/* ; cp src/* build",
    "encrypt": "node --env-file=.env scripts/encrypt.js",
    "watch": "npx tailwindcss -i ./src/input.css -o ./src/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.10",
    "@tailwindcss/forms": "^0.5.9"
  }
}
